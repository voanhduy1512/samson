sqlite: &sqlite
  adapter: sqlite3
  database: ":memory:"

mysql: &mysql
  adapter: mysql2
  username: root

postgresql: &postgresql
  adapter: postgresql
  username: postgres

defaults: &defaults
  pool: 5
  <<: *<%= ENV['DB'] || "sqlite" %>

test:
  database: samson_test
  <<: *defaults
